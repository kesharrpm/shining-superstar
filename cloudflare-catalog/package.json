{
  "name": "shining-superstar-catalog-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:apply:local": "wrangler d1 execute shining-superstar-catalog --local --file=./schema.sql",
    "db:apply:remote": "wrangler d1 execute shining-superstar-catalog --remote --file=./schema.sql"
  },
  "dependencies": {
    "@zip.js/zip.js": "^2.7.72"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
